#version 330
precision highp float;

in vec3 out_Normal;
out vec4 out_Color;
//uniform sampler2D Texture;

void main(void)
{
	/*vec4 TexureColor = texture2D( Texture, out_Texture );
	
	if( TexureColor.a == 0.0 )
	{
		discard;
	}*/
	
	out_Color = vec4( out_Normal, 0.5 );//TexureColor;
}