#version 330
precision highp float;

in vec2 out_Texture;
out vec4 out_Color;
uniform sampler2D Texture;

void main(void)
{
	vec4 TexureColor = texture2D( Texture, out_Texture );
	
	/*if( TexureColor.a == 0.0 )
	{
		discard;
	}*/
	
	out_Color = vec4( 1.0, 1.0, 1.0, 1.0f ); //TexureColor;
}